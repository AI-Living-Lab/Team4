# ============================================================
# paths.env - 실제 경로 설정 (git에 올라가지 않음)
# paths.env.example을 복사해서 수정하세요
# ============================================================

# 프로젝트 루트
BASE_DIR=/root/hyj/Team4

# 데이터 / 캐시
DATA_DIR=/root/datasets
HF_HOME=/root/.cache/huggingface


# 체크포인트 저장 디렉토리
CHECKPOINTS_DIR=/root/checkpoints

# SFT 체크포인트 (checkpoints-26000 폴더)
# SFT_CKPT=/root/checkpoints/checkpoint-26000
SFT_CKPT=/workspace/checkpoints/adapter

# 베이스 모델 (HuggingFace model ID 또는 로컬 경로)
# 원본: /mnt/bn/tiktok-mm-4/aiic/public/model/OV-Qwen2-7B-AM9
# BASE_MODEL=lmms-lab/llava-onevision-qwen2-7b-ov  (동일)
BASE_MODEL=/workspace/checkpoints/base

# SALMONN2 사전학습 체크포인트 (video_salmonn2_hf)
SALMONN2_CKPT=${CHECKPOINTS_DIR}/video_salmonn2_hf

# UnAV-100 annotation JSON
UNAV_ANNO=${DATA_DIR}/unav100_annotations.json

# UnAV-100 비디오/오디오 원본 (데이터 전처리 시 필요)
UNAV_VIDEO_DIR=${DATA_DIR}/unav_100/videos
UNAV_AUDIO_DIR=${DATA_DIR}/unav_100/audio


# PU-VALOR 데이터셋
PUVALOR_DIR=${DATA_DIR}/PU-VALOR
VALOR_DIR=${DATA_DIR}/VALOR-32K/valor_videos

# CLAP 모델 (eval/parse_and_eval.py에 필요)
CLAP_SRC=${BASE_DIR}/CLAP/src
CLAP_CKPT=${BASE_DIR}/CLAP/630k-audioset-fusion-best.pt

# LoRA 머지 출력 디렉토리 (merge_lora_vs2.py에 필요)
MERGE_OUT_DIR=${CHECKPOINTS_DIR}/merged
