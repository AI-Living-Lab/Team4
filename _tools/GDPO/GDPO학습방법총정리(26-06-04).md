# GDPO 학습 가이드 (VS2+ / Qwen2.5-VL)

`gdpo_trainer.py` 로 **video_SALMONN2_plus (VS2+)** 모델을 GDPO(GRPO 변형) 강화학습하는 방법을 정리한 문서입니다.
SFT 로 학습한 multi-segment temporal grounding 정책을, temporal-IoU 기반 reward 로 RL 미세조정합니다.

---

## 1. GDPO 개요

GDPO 는 GRPO(Group Relative Policy Optimization)를 VS2+ 멀티모달(비디오+오디오) temporal grounding 에 맞춰 구현한 트레이너입니다.

학습 1 step 의 흐름:

1. **Generate** — 한 샘플 프롬프트로 `num_generations`(기본 8)개의 답변을 샘플링 (`do_sample`, `temperature`, `top_k=50`).
2. **Reward** — 각 답변을 GT 시간구간과 비교해 reward 계산. reward 채널 = `[format, iou]`.
   - `format` : `"From <tX><tX><tX><tdot><tX> to ... ."` 포맷 준수도.
   - `iou`    : MUSEG `r_M`(segment-matching NGIoU) 등 temporal grounding 점수.
3. **Advantage** — 그룹 내 reward 를 정규화(`(r - mean) / (std + eps)`)해 GDPO advantage 계산.
4. **Loss** — policy log-prob × advantage − `beta` × KL(ref 정책 대비). PPO-clip(`use_grpo=false`) 도 지원.

reward 형식은 답변이 아래 토큰 포맷을 따른다고 가정합니다 (UnAV-100 multi-segment QA):

```
From <t0><t0><t3><tdot><t3> to <t0><t0><t5><tdot><t6>. From <t0><t0><t9><tdot><t1> to <t0><t1><t0><tdot><t0>.
```

---

## 2. 디렉터리 구성

```
_tools/GDPO/
├── gdpo_trainer.py            # ★ 메인 트레이너 (이 문서의 대상)
├── config.yaml                # 하이퍼파라미터 설정
├── reward_functions.py        # 기본 reward (format + MUSEG r_M)
├── reward_functions_rM_fp.py  # r_M + false-positive 페널티
├── reward_functions_prec.py   # r_M + 시간단위 precision 페널티
├── reward_functions_rM_cov.py # r_M + coverage-excess 페널티
├── reward_functions_f1.py     # F1(best-match) 베이스라인
├── reward_functions_rM_fp_cot.py  # CoT(<think>/<answer>) 포맷용 reward
├── gdpo_trainer_f1_cot.py     # CoT 학습용 트레이너 변형
└── README.md                  # 이 문서
```

---

## 3. 사전 준비

### 3.1 환경

VS2+ conda 환경이 활성화돼 있어야 합니다.

```bash
source /workspace/setup.sh && conda activate salmonn2p
```

`gdpo_trainer.py` 는 프로젝트 루트와 `video_SALMONN2_plus/` 를 `sys.path` 에 자동 추가하므로,
`qwenvl.model.modeling_qwen2_5_vl` / `qwenvl.data.dataset` import 가 가능해야 합니다.

### 3.2 경로 / 환경변수 (`paths.env`)

`Team4/paths.env` 의 공통 경로(`CKPT_DIR` / `TRAIN_DIR` / `WANDB_*` 등)는 이미 환경에 export 돼 있어 그대로 쓰면 됩니다 (`source` 불필요).

> 이 문서는 **모델 경로를 CLI 인자(`--model_path` / `--model_base`)로 직접 넘기는 방식**을 기본으로 합니다.
> 즉 `SFT_CKPT` / `BASE_MODEL` 같은 환경변수를 따로 export 할 필요가 없습니다.
> (config.yaml 의 `${SFT_CKPT}` / `${BASE_MODEL}` placeholder 는 CLI 인자가 그대로 덮어씁니다.)

자주 쓰는 경로 (CLI 인자에 그대로 붙여 사용):

| 경로 | 예시 값 |
|------|---------|
| `model_path` (SFT adapter 또는 머지 모델) | `${CKPT_DIR}/sft/salmonn2p_7b_unav_fps5_off` |
| `model_base` (VS2+ time-token base) | `${CKPT_DIR}/video_salmonn2_plus_7B_time_tokens` |
| `dataset_path` (학습 JSON) | `${TRAIN_DIR}/unav100_v2.json` |

### 3.3 데이터셋

`${TRAIN_DIR}/unav100_v2.json` 형식의 UnAV-100 multi-segment QA JSON.
각 항목은 `video` / `audio` / `conversations`(human 질문 + gpt 시간구간 답변) 를 가집니다.
GT 시간구간은 학습 중 `gpt` 답변의 `From <t..> to <t..>` 패턴에서 자동 파싱됩니다.

---

## 4. 두 가지 학습 경로 (자동 분기)

`model_path` 가 무엇이냐에 따라 트레이너가 자동으로 경로를 선택합니다.

### 경로 A — SFT adapter 이어학습
- `model_path` = **SFT LoRA adapter 디렉터리** (`adapter_config.json` 존재)
- `model_base` = time-token base 모델
- 동작: adapter 를 base 위에 로드 후 **RL 학습대상을 제한** — `LoRA(q/k/v)` + `embed_tokens` + `lm_head` 만 학습. `o_proj` LoRA / `visual.merger` / `audio.*` 는 freeze (SFT 멀티모달 grounding 보호).
- ref 모델: adapter `disable_adapter()` = pre-SFT base.
- ⚠️ 이 경로에서는 **`config.lora.enabled` 를 `false`** 로 둘 것 (이미 PeftModel 에 LoRA 이중 적용 방지).

### 경로 B — fresh LoRA (config.yaml 기본값)
- `model_path` = `model_base` = **SFT-머지 모델** (`adapter_config.json` 없음)
- 동작: 머지 weight 위에 **새 RL LoRA** 를 씌움 (`config.lora` 설정 사용). 비-LoRA 파라미터는 자동 freeze.
- ref 모델: LoRA `disable_adapter()` = SFT 정책 (KL 기준이 SFT).
- `config.lora.enabled: true` 필요.

---

## 5. `config.yaml` 설정

```yaml
model:
  model_path: ${SFT_CKPT}     # placeholder — 보통 CLI --model_path 로 덮어씀 (경로A=adapter / 경로B=머지모델)
  model_base: ${BASE_MODEL}   # placeholder — 보통 CLI --model_base 로 덮어씀 (time_tokens 포함 VS2+ base)
  tti_mode: "off"             # off | on  (§7 참고)

data:
  dataset_path: ${TRAIN_DIR}/unav100_v2.json

lora:                         # 경로 B(fresh LoRA)에서만 사용
  enabled: true               # 경로 A 면 false 로!
  r: 32
  lora_alpha: 64
  lora_dropout: 0.05
  train_embeddings: true      # embed_tokens/lm_head 도 학습 (modules_to_save)

gdpo:
  num_generations: 8          # 그룹당 샘플 수 (advantage 정규화 단위)
  max_completion_length: 256  # 생성 최대 토큰
  beta: 0.04                  # KL 계수 (0 이면 ref 모델 비활성)
  temperature: 1.5            # 샘플링 다양성
  reward_weights: [1.0, 1.0]  # [format, iou] 가중치

training:
  num_train_epochs: 1
  max_steps: 1000             # >0 이면 epoch 무시 (smoke-test/길이제한)
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 4
  learning_rate: 5e-6
  warmup_ratio: 0.05
  lr_scheduler_type: cosine
  bf16: true
  seed: 2024
  gradient_checkpointing: true
  gradient_checkpointing_kwargs:
    use_reentrant: false

logging:
  logging_steps: 1
  save_steps: 100
  save_total_limit: 5
  report_to: wandb            # tensorboard 도 가능

ref_model:
  enabled: true
```

> CLI 인자가 config 보다 우선합니다(`--model_path`, `--max_steps`, `--tti_mode`, `--reward_module`, `--run_name` 등).

---

## 6. 실행 방법

### 6.1 단일 GPU

모델/데이터 경로를 **CLI 인자로 직접 전달**합니다 (별도 export 불필요).

```bash
cd /workspace/cdh/Team4
conda activate salmonn2p

python _tools/GDPO/gdpo_trainer.py \
    --config       _tools/GDPO/config.yaml \
    --model_path   ${CKPT_DIR}/sft/salmonn2p_7b_unav_fps5_off \
    --model_base   ${CKPT_DIR}/video_salmonn2_plus_7B_time_tokens \
    --dataset_path ${TRAIN_DIR}/unav100_v2.json
```

> `${CKPT_DIR}` / `${TRAIN_DIR}` 도 풀어 쓰고 싶으면 절대경로를 그대로 넣으면 됩니다. 예:
> `--model_base /workspace/checkpoints/video_salmonn2_plus_7B_time_tokens`

### 6.2 멀티 GPU (torchrun)

```bash
export CUDA_VISIBLE_DEVICES=0,1,2,3
torchrun --standalone --nproc_per_node=4 \
    _tools/GDPO/gdpo_trainer.py \
    --config       _tools/GDPO/config.yaml \
    --model_path   ${CKPT_DIR}/sft/salmonn2p_7b_unav_fps5_off \
    --model_base   ${CKPT_DIR}/video_salmonn2_plus_7B_time_tokens \
    --dataset_path ${TRAIN_DIR}/unav100_v2.json
```

### 6.3 모든 인자 명시 (config 값 덮어쓰기)

```bash
python _tools/GDPO/gdpo_trainer.py \
    --config       _tools/GDPO/config.yaml \
    --model_path   ${CKPT_DIR}/sft/salmonn2p_7b_unav_fps5_off \
    --model_base   ${CKPT_DIR}/video_salmonn2_plus_7B_time_tokens \
    --dataset_path ${TRAIN_DIR}/unav100_v2.json \
    --output_dir   ${CKPT_DIR}/gdpo/salmonn2p_7b_unav_rM \
    --reward_module reward_functions \
    --tti_mode     off \
    --max_steps    1000 \
    --run_name     gdpo_rM_run1
```

### 6.4 빠른 동작 확인 (smoke test)

```bash
python _tools/GDPO/gdpo_trainer.py \
    --config       _tools/GDPO/config.yaml \
    --model_path   ${CKPT_DIR}/sft/salmonn2p_7b_unav_fps5_off \
    --model_base   ${CKPT_DIR}/video_salmonn2_plus_7B_time_tokens \
    --dataset_path ${TRAIN_DIR}/unav100_v2.json \
    --max_steps 3
```

### 6.5 체크포인트에서 재개

재개 시에도 모델/데이터 경로는 동일하게 넘겨줍니다 (모델을 먼저 로드한 뒤 체크포인트 weight 를 덮어쓰기 때문).

```bash
# output_dir 안의 최신 체크포인트 자동 재개
python _tools/GDPO/gdpo_trainer.py \
    --config       _tools/GDPO/config.yaml \
    --model_path   ${CKPT_DIR}/sft/salmonn2p_7b_unav_fps5_off \
    --model_base   ${CKPT_DIR}/video_salmonn2_plus_7B_time_tokens \
    --dataset_path ${TRAIN_DIR}/unav100_v2.json \
    --output_dir   ${CKPT_DIR}/gdpo/salmonn2p_7b_unav_rM \
    --resume_from_checkpoint True

# 특정 체크포인트 지정
python _tools/GDPO/gdpo_trainer.py \
    --config       _tools/GDPO/config.yaml \
    --model_path   ${CKPT_DIR}/sft/salmonn2p_7b_unav_fps5_off \
    --model_base   ${CKPT_DIR}/video_salmonn2_plus_7B_time_tokens \
    --dataset_path ${TRAIN_DIR}/unav100_v2.json \
    --resume_from_checkpoint ${CKPT_DIR}/gdpo/salmonn2p_7b_unav_rM/checkpoint-500
```

### 6.6 백그라운드 실행 (SSH 끊겨도 유지: `setsid nohup`)

장시간 학습은 SSH 가 끊겨도 죽지 않게 백그라운드로 띄웁니다.
`setsid` 로 새 세션에 분리하고 `nohup` 으로 HUP 시그널을 무시합니다.

> 학습 로그는 trainer 가 `output_dir` 에 **`train.log`** 로 남기므로 별도 리다이렉트가 필요 없습니다.
> 콘솔 출력은 `/dev/null` 로 버립니다.

```bash
cd /workspace/cdh/Team4
conda activate salmonn2p

OUT=${CKPT_DIR}/gdpo/salmonn2p_7b_unav_rM

setsid nohup python _tools/GDPO/gdpo_trainer.py \
    --config       _tools/GDPO/config.yaml \
    --model_path   ${CKPT_DIR}/sft/salmonn2p_7b_unav_fps5_off \
    --model_base   ${CKPT_DIR}/video_salmonn2_plus_7B_time_tokens \
    --dataset_path ${TRAIN_DIR}/unav100_v2.json \
    --output_dir   "$OUT" \
    < /dev/null > /dev/null 2>&1 &

echo "started pid=$!"
```

멀티 GPU(torchrun)도 동일하게 앞에 `setsid nohup` 을 붙입니다.

```bash
export CUDA_VISIBLE_DEVICES=0,1,2,3
setsid nohup torchrun --standalone --nproc_per_node=4 \
    _tools/GDPO/gdpo_trainer.py \
    --config       _tools/GDPO/config.yaml \
    --model_path   ${CKPT_DIR}/sft/salmonn2p_7b_unav_fps5_off \
    --model_base   ${CKPT_DIR}/video_salmonn2_plus_7B_time_tokens \
    --dataset_path ${TRAIN_DIR}/unav100_v2.json \
    --output_dir   "$OUT" \
    < /dev/null > /dev/null 2>&1 &
```

실행 후 관리:

```bash
tail -f "$OUT/train.log"                      # 학습 로그 실시간 확인
ps -ef | grep gdpo_trainer | grep -v grep     # 살아있는지 확인
nvidia-smi                                     # GPU 점유 확인
pkill -f gdpo_trainer.py                       # 종료 (torchrun 자식까지)
```

---

## 7. CLI 인자 요약

| 인자 | 설명 | 기본값(미지정 시) |
|------|------|-------------------|
| `--config`        | config.yaml 경로 | (없음) |
| `--model_path`    | SFT adapter(경로 A) 또는 머지 모델(경로 B) | `config.model.model_path` |
| `--model_base`    | VS2+ time-token base | `config.model.model_base` |
| `--dataset_path`  | 학습 JSON | `config.data.dataset_path` |
| `--output_dir`    | 체크포인트/로그 저장 경로 | `config.training.output_dir` 또는 `output/gdpo_vs2plus` |
| `--max_steps`     | step 수 제한(>0 이면 epoch 무시) | `config.training.max_steps` |
| `--run_name`      | wandb/tracker run 이름 | `config.logging.run_name` 또는 output_dir basename |
| `--tti_mode`      | `off` / `on` (§8) | `config.model.tti_mode` 또는 `off` |
| `--reward_module` | reward 모듈명 (§9) | `config.reward.module` 또는 `reward_functions` |
| `--resume_from_checkpoint` | `True`=자동 / 경로=특정 / 미지정=처음부터 | None |

> `--model_path` 와 `--dataset_path` 는 (config 포함) 반드시 채워져야 합니다. 없으면 에러로 종료합니다.

---

## 8. `tti_mode` (Time-Token Interleaving)

모델 config(`time_token_id_range`)와 데이터셋 입력(`tti_time_format`)이 **반드시 짝이 맞아야** 합니다.

| `tti_mode` | 모델 config | 데이터 마커 | 사용 상황 |
|------------|-------------|-------------|-----------|
| `off` | `time_token_id_range = None` 으로 강제 | 마커 미삽입 (`tti_time_format="off"`) | 데이터에 time marker 인터리빙이 없을 때 (기본) |
| `on`  | base config 유지 / 머지모델은 토크나이저에서 복원 | `special_token` 인터리빙 | 비디오/오디오 청크 사이에 time marker 가 삽입된 데이터 |

`off` 인데 base 에 `time_token_id_range` 가 남아 있으면 rope_index 가 항상 ON 분기를 타 `IndexError` 가 날 수 있어, 트레이너가 자동으로 `None` 처리합니다. 두 값이 어긋나면(desync) 경고가 출력됩니다.

---

## 9. Reward 모듈 선택

`--reward_module`(또는 `config.reward.module`)로 trainer 복제 없이 reward 구현만 교체합니다.
모든 모듈은 `format_reward` + `iou_reward` 를 export 합니다 (`iou` 채널 구현만 다름).

| 모듈 | `iou` 구현 | 특징 |
|------|-----------|------|
| `reward_functions` (기본) | MUSEG `r_M` = ½(r_G + r_L) | union-global NGIoU + 위치순 local NGIoU 하이브리드 |
| `reward_functions_rM_fp`  | `r_M − K·n_unmatched_pred` | false-positive(과예측 세그먼트 수) 페널티 → precision 복원 |
| `reward_functions_prec`   | `r_M − λ·outside_ratio` | 시간단위 precision 페널티 → blanket 한 통 collapse 방지 |
| `reward_functions_rM_cov` | `r_M − γ·coverage_excess` | GT 대비 덮은 총시간 초과분 페널티 → 길이편향(통짜) 억제 |
| `reward_functions_f1`     | F1(best-match) | Recall/Precision F1 베이스라인 (비교용) |
| `reward_functions_rM_fp_cot` | r_M + FP (CoT 채널) | `<think>/<answer>` 포맷 답변용 — `gdpo_trainer_f1_cot.py` 와 함께 |

> CoT(`<think>...</think><answer>...</answer>`) 데이터(`unav100_v2_cot.json`)로 학습할 때는
> `gdpo_trainer_f1_cot.py` + `reward_functions_*_cot` 조합을 사용합니다.

---

## 10. 출력물

`output_dir` 아래에 저장됩니다.

- `checkpoint-XXXX/` — `save_steps` 마다 저장 (`save_total_limit` 개 유지).
- 최종 모델 — 학습 종료 후 `trainer.save_model(output_dir)`.
- `cli_args.json` (재실행 시 `cli_args.1.json`, …) — 실행한 CLI 인자/커맨드라인/해석된 경로 기록(재현용).
- `logs/` — tensorboard 로그 (`report_to=tensorboard` 일 때). wandb 사용 시 `paths.env` 의 `WANDB_*` 키 적용.

학습 중 rank-0 로그(예시):

```
[GDPO STEP]    prompt_len=..., actual_lengths=[...], comp_len=...
[GDPO SAMPLE]  completion[0][:200]: From <t0>...
[GDPO SAMPLES] GT sec:  (3.3, 5.6), (9.1, 10.0), ...
  [0] iou=0.742
       raw: from <t0><t0><t3><tdot><t3> to ...
       sec: (3.3, 5.6), ...
```

---

## 11. 디버깅

### TTI 계측

```bash
TTI_DEBUG=1 TTI_DEBUG_STEPS=3 python _tools/GDPO/gdpo_trainer.py --config _tools/GDPO/config.yaml
```

`[TTI-DBG]` 로그로 `model.config.time_token_id_range` ↔ `tti_time_format` 정합성, 프롬프트 내 time-marker 개수, vision_start 주변 인터리빙을 첫 N step 동안 확인합니다.

### 자주 겪는 이슈

- **`IndexError` (rope_index)** — `tti_mode=on` 인데 데이터에 마커가 없거나, base config 의 `time_token_id_range` 와 데이터가 desync. `tti_mode` 와 데이터셋을 맞추세요.
- **LoRA 이중 적용 경고** — 경로 A(adapter)인데 `config.lora.enabled: true`. `false` 로 변경.
- **OOM** — `num_generations` / `max_completion_length` 축소, `gradient_checkpointing: true` 유지(긴 멀티모달 시퀀스에 필수).
- **resume 시 RNG `UnpicklingError`** — 트레이너가 numpy global allowlist 로 자동 우회합니다(`torch>=2.4` 대응).

---

## 빠른 시작 (요약)

```bash
cd /workspace/cdh/Team4
conda activate salmonn2p

python _tools/GDPO/gdpo_trainer.py \
    --config       _tools/GDPO/config.yaml \
    --model_path   ${CKPT_DIR}/sft/salmonn2p_7b_unav_fps5_off \
    --model_base   ${CKPT_DIR}/video_salmonn2_plus_7B_time_tokens \
    --dataset_path ${TRAIN_DIR}/unav100_v2.json
```
